# 项目开发约束

## 一、先保证数据与功能完整,再谈性能

- **优先级是死的:数据完整 > 功能正确 > 性能。** 任何优化只要可能让某一维数据
  变少、变旧、变不准,就不做——哪怕它更快、更省内存。缺一段数据用户是能看见的,
  快 200 毫秒用户看不见。
- **只做说得明白的性能优化。** 收益和副作用都要落到可观测量上:命中率、P95、
  吞吐、峰值内存、上游请求数、积分消耗、数据陈旧度。说不清量级的,不做;
  量出来收益和副作用相当的,不做。判据见第六条。
- **落地前先做分析调研,把这三件事写下来再动手**:这一项优化省的是什么(哪个
  可观测量、省多少)、代价落在哪(同一条链路上还有哪些阈值会跟着变)、以及
  它会不会碰数据完整性。三件里有一件写不出来,就先别写代码。
- 调研结论是"不值得改"同样是有效产出,写进注释或文档即可,不必硬改。真实例子:
  兜底 K 线对同一标的取两次(复权 + 不复权),看着能省一次,但不复权序列喂着
  市盈率(静),省掉会在钉日期的查询里把它算错——查清楚、记下来、不动。
- 不要为省资源去换一条数据更差的路。真实例子:同花顺的鉴权用有头浏览器,实测
  无头只有 12% 能拿到 cookie、有头 100%;为了省一个浏览器实例换无头,等于用
  市场宽度这一维去换内存。

## 二、取数走同一套架构:逐级回退、插拔式、可交叉合成

每一维数据都由一层可插拔的 provider 提供,不许再往调用链里塞 if/else。四条要求:

- **注册表 + 配置顺序。** provider 用 `register()` 挂进注册表,启用哪些、按什么顺序,
  由一个环境变量决定(`BASIC_INFO_PROVIDERS` 这种),置 `off` 能关掉整层。加一个源
  不用改调用链,临时禁用某个源不用改代码。
- **接新源的成本必须是"写一个类 + 注册一行"。** 后面还会接同花顺、雪球这些,判据是:
  加一个源不用动任何既有文件的既有代码——不改调用链、不改别的 provider、不加 if/else。
  做不到就说明抽象没提对。provider 自己声明 `supports(symbol)`,不支持的标的直接跳过、
  不发请求(腾讯对北交所是 KeyError,靠 try/except 兜等于每次白付一个往返);
  自己声明能给哪些字段,让合成层知道该找谁补。
- **逐级回退。** 前一个源没给出结果就试下一个,不是"失败才试",空结果同样往下走。
  每一级失败都要留下能定位到源的日志——查一个数是谁给的,不该靠猜。
- **能交叉合成就合成,别只取第一个。** provider A 给了字段 a、B 给了字段 b,合起来
  返回一份完整的,而不是因为 A 先返回就丢掉 b。已经有值的不被后来的覆盖(先配置的
  优先级更高),来源记成 `A+B` 让日志看得出这份数据是拼的。`basic_info.resolve()`
  是参照实现:没有它,没网关的部署会连带丢掉市盈率(静)和换手率。
- **合成解决不了冲突,别指望它。** 字段级合并解的是"A 缺 a、B 有 a";两个源对同一
  字段给出不同的值,那是仲裁问题,归第一条管——按配置顺序信前面那个,并把差异记进
  已知差异清单。真实例子:创业板指的成交量东财对、腾讯和新浪都低 3.5%,两家同源,
  合并多少次也变不出对的数。

重构成这套架构属于纯改造,**行为必须逐字等价**:改动前后用同一组调用做输出哈希比对,
覆盖钉日期的历史查询和收盘后的实时查询两类,比对时要先关掉报告缓存——不然"后"那次
读的是"前"那次的字节,等价性是假的。

## 三、并发性能优先

- 在第一条的前提下,性能目标是降低 MCP 调用耗时,尤其是多标的、多批次并发下的
  P95 和最长耗时。
- 性能验证必须覆盖单批 4 个标的,以及同时 5 批、10 批(每批 4 个标的)的场景,比较端到端耗时、P90/P95、最大耗时、吞吐和错误率。
- 优化不得改变业务逻辑和返回数据;改动前后需用同一组请求做输出比对(逐字或哈希)证明等价。
- 优先消除排队、重复请求和串行等待;避免无界线程、任务或队列。以生产日志为依据,用最小改动换取可量化的收益。

## 四、峰值内存不超过 500 MiB

- 所有优化以峰值内存不超过 500 MiB 为前提,超限的性能提升一律不采纳。
- 按 MCP 服务及其 Chromium、Xvfb 等子进程的合计峰值统计。
- 缓存、队列、连接池、线程和进程都必须有明确上限与回收策略。

## 五、Ubuntu 兼容优先

- 设计、依赖、脚本和验证优先保证 Ubuntu 兼容,不依赖 macOS 专属能力。
- 发布前在目标 Ubuntu 环境验证启动、停止、并发、浏览器回退和内存峰值。

## 六、改动前先算收益与副作用

- 改关键配置项、加缓存/回退/熔断、调并发上限,都要先给出收益的量级估计,再列副作用,
  并说明为什么收益大于副作用。收益说不清量级的,不改。
- 收益和副作用都要落到可观测量上:命中率、P95、吞吐、峰值内存、上游请求数、积分消耗、
  数据陈旧度。不能只说"更快"或"更稳"。
- 优先用生产日志或归档回放来估收益,而不是推理。已有机制可能已经把收益吃掉了——例如
  `FUND_FLOW_PAGE_REUSE_SECONDS` 上线后,`FALLBACK_WAIT_SECONDS` 的超时在全部日志里
  一次都没触发过,再调它是零收益。
- 副作用要连带检查同一条链路上的其它阈值。放宽一处等待,可能让下游的失败计数更快凑够
  熔断阈值,把"少一段数据"变成"整层停摆"。
- 收益与副作用相当或无法判定时,保持默认值不动,并把结论写进注释或文档,免得下次重来。

## 七、提交信息精简、不带署名

- 禁止在 commit message 里添加任何署名或协作者标记,例如
  `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`、
  `Generated with ...`、`Signed-off-by` 等 trailer。
- **写短。** 一行标题说清改了什么;正文最多三五行,只留判断依据(关键实测数字、
  为什么是这个值)。不要长篇论证、不要贴时间线、不要复述代码。
- 只有 breaking change 才允许写长:必须写明破坏了什么、调用方要怎么改。
- 详细的推导、实测过程和取舍,写进代码注释或文档——那里才会被下一个人读到,
  而 commit message 是给 `git log` 扫一眼用的。
