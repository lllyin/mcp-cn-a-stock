# 项目开发约束

## 性能优先

- 优先降低 MCP 调用耗时和长尾，并提高并发吞吐；性能优化不得改变既有输出语义。
- 性能验证必须覆盖单批 4 个标的，以及同时 5 批、10 批（每批 4 个标的）的场景。
- 重点比较端到端耗时、P90/P95、最大耗时、吞吐和错误率，避免无界线程、任务或队列。

## 优化边界

- 优先解决生产日志或稳定复现确认的已知问题，其次解决在受支持场景下必然发生的问题。
- 不为仅有理论可能、尚无证据或无法稳定复现的情况增加防御性分支、抽象或兜底。
- 每次优化采用能解决问题的最小改动，并用测试或数据证明正收益；没有明确收益时保持现状。

## 内存上限

- 所有优化必须以峰值内存不超过 500 MiB 为前提。
- 统计 MCP 服务及其 Chromium、Xvfb 等子进程的合计峰值内存；不得用增加进程、线程、缓存或队列换取超限的性能提升。

## Ubuntu 优先

- 设计、依赖、脚本和验证优先保证 Ubuntu 兼容性，不依赖 macOS 专属能力。
- 发布前应在目标 Ubuntu 环境验证启动、停止、并发、浏览器回退和内存峰值。
